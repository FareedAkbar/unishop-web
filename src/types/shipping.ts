
  export default interface ShippingType {
    amount: number; // Just use number; 0 is included
    label: string; // Just use string; "" is included
    type: string; // Just use string; "" is included
    value: string; // Just use string; "" is included
    
  }
  